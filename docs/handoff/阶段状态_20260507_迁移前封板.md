# 阶段状态：2026-05-07 迁移前封板

## 1. GitHub 同步

已提交并推送：

```text
5367eef chore: seal migration handoff and add facegan studio
```

远端：

```text
origin/main
https://github.com/Xiong-z-x/GAN-.git
```

本地状态：

```text
main...origin/main
working tree clean
```

## 2. 本地验证

已执行并通过：

```text
python -m compileall src scripts facegan_studio tests
python -m unittest tests.test_facegan_studio_core -v
python -m facegan_studio.app --help
git diff --cached --check
```

结果：

- Python 语法编译通过。
- FaceGAN Studio 轻量测试 5 项通过。
- FaceGAN Studio CLI 参数帮助正常输出。
- 暂存区格式检查通过。
- 提交后本地未发现 `__pycache__`、`.pytest_cache`、压缩包或临时下载残留。

## 3. AutoDL 只读检查

尝试执行：

```powershell
ssh autodl-gan "cd /root/autodl-tmp/GAN 2>/dev/null && git rev-parse --short HEAD && git status --short --branch || echo NO_GAN_GIT_DIR"
```

结果：

```text
ssh: connect to host connect.westc.seetacloud.com port 34067: Connection refused
```

判断：

- 这不是源码封板失败。
- 更可能是 AutoDL 实例未运行、端口已变化或平台转发暂不可用。
- 下一次开机后应先在本地执行 `ssh autodl-gan "hostname && pwd"` 验证连接，再在 AutoDL 中 `git pull --ff-only`。

## 4. 下次接手第一步

AutoDL 实例可连接后，若 `/root/autodl-tmp/GAN` 不存在，先执行：

```bash
cd /root/autodl-tmp
git clone https://github.com/Xiong-z-x/GAN-.git GAN
cd /root/autodl-tmp/GAN
```

若目录已存在，执行：

```bash
cd /root/autodl-tmp/GAN
git status --short --branch
git pull --ff-only
sed -i 's/\r$//' scripts/*.sh
chmod +x scripts/*.sh
python -m compileall src scripts facegan_studio
python -m facegan_studio.app --help
```

如果 `git pull --ff-only` 失败，不要强制覆盖；先查看 `git status --short` 并判断本地改动是否需要保留。

如果 GitHub 暂时不可用，使用 `docs/handoff/AutoDL_新工作区启动指令.md` 中的本地 `scp` 兜底方案，只复制源码、docs、脚本和轻量交接素材，不复制大结果目录、数据集、外部仓库和个人照片。

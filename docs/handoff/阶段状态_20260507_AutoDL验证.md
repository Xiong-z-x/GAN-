# 阶段状态：2026-05-07 AutoDL 验证

## 1. 工作区与仓库

事实：

- 当前工作区：`/root/autodl-tmp/GAN`
- 当前提交：`46514d9 docs: add autodl bootstrap handoff path`
- `git pull --ff-only` 输出：`Already up to date.`
- 新会话开始时 `/root/autodl-tmp/GAN` 已有源码文件但不是 Git 仓库。
- 已将旧非 Git 目录备份为：`/root/autodl-tmp/GAN_nogit_backup_20260507_175203`
- 已从 GitHub 克隆正式仓库到 `/root/autodl-tmp/GAN`。
- 因 `/root/autodl-tmp` 只有 50G，直接复制旧 `data/` 和 `outputs/` 导致 `No space left on device`。后来只清理新仓库里重复的 ignored `data/outputs` 半成品，旧备份保留。
- 旧备份中的 `external/` 已移动到新仓库 `external/`，避免复制出第二份 9.9G 外部仓库。

当前磁盘关键事实：

- `/root/autodl-tmp` 清理后约 75% 使用率，约 13G 可用。
- `/autodl-fs/data` 约 194G 可用。

## 2. 已阅读文档

已按顺序阅读：

1. `README.md`
2. `docs/handoff/AutoDL_新工作区启动指令.md`
3. `docs/handoff/新对话初始化提示词.md`
4. `docs/handoff/迁移前交接总报告.md`
5. `docs/handoff/后续模型注意事项.md`
6. `AutoDL_运行指南.md`
7. `FaceGAN_Studio_运行说明.md`
8. `代码附录.md`
9. `文件说明.md`
10. `报告素材使用说明.md`

用户上传的 `/root/autodl-tmp/GAN_AutoDL_handoff_md_20260507_123808.zip` 已确认存在，但仓库内 md 文件已经可读，本轮优先使用仓库真实文件。

## 3. 环境与依赖

已验证：

```text
python -m compileall src scripts facegan_studio
python -m facegan_studio.app --help
python -m unittest tests.test_facegan_studio_core -v
python -m pip check
```

结果：

- `compileall` 通过。
- `facegan_studio.app --help` 通过。
- `tests.test_facegan_studio_core` 当前 7 项通过。
- `pip check` 输出：`No broken requirements found.`

本轮踩坑：

- 直接 `pip install gradio` 安装了 `gradio 6.14.0`，并把 `huggingface-hub` 升到 `1.14.0`，与当前 `transformers 4.37.1` / `tokenizers 0.15.2` 冲突。
- 已收敛到兼容组合：
  - `gradio 4.44.1`
  - `gradio-client 1.3.0`
  - `fastapi 0.112.2`
  - `starlette 0.38.6`
  - `pydantic 2.10.6`
  - `huggingface-hub 0.24.7`
  - `diffusers 0.25.1`
  - `transformers 4.37.1`
  - `torch 2.8.0+cu128`
- 已将关键版本 pin 到 `requirements_autodl.txt`。

## 4. FaceGAN Studio 页面验证

启动命令：

```bash
FACEGAN_PORT=7860 bash scripts/run_facegan_studio.sh
```

页面验证：

- 普通 `curl http://127.0.0.1:7860/` 会被环境代理影响，返回代理侧 502。
- 使用 `curl --noproxy '*' http://127.0.0.1:7860/` 返回 `HTTP/1.1 200 OK`。
- `/config` 可读取到 `FaceGAN Studio`。2026-05-07 后续按用户要求移除了证件照页面，并新增 GFPGAN 与轻造型保脸入口。当前页面 API：
  - `/preview_face`
  - `/run_anime`
  - `/run_pose`
  - `/run_accessories`
  - `/run_gfpgan`
  - `/load_showcase`

## 5. 成果展示页

当前 `docs/handoff_assets/` 内 5 张 PNG 均可用：

- `dcgan_evolution_grid.png` `(1260, 888)`
- `my_face_pose_grid_4x4.png` `(1024, 1024)`
- `pose_reference_grid_4x4.png` `(1024, 1024)`
- `stylegan3_top16_grid.png` `(1024, 1072)`
- `stylegan3_top32_grid.png` `(1536, 816)`

`collect_showcase_assets('/root/autodl-tmp/GAN')` 当前返回 4 个展示项：

- DCGAN 演化展示
- StyleGAN3 高质量人像
- InstantID 姿态参考
- InstantID 身份保持结果

## 6. 上传预览

测试输入：

```text
outputs/facegan_studio/manual_verify/light_modules/test_upload_stylegan3_crop.png
```

来源：从 `docs/handoff_assets/stylegan3_top16_grid.png` 裁出单脸样例，尺寸 `(512, 512)`。

页面 API 验证：

- `/preview_face` 返回：`已使用 OpenCV 检测到人脸。`
- 预览图尺寸：`(512, 512)`

## 7. AnimeGANv2 / CycleGAN

外部仓库检查：

- `external/animegan2-pytorch/test.py` 存在。
- AnimeGANv2 权重存在：
  - `face_paint_512_v2.pt`
  - `face_paint_512_v1.pt`
  - `paprika.pt`
  - `celeba_distill.pt`
- `external/pytorch-CycleGAN-and-pix2pix/test.py` 存在。
- CycleGAN `style_vangogh_pretrained/latest_net_G.pth` 已恢复到完整 `45,575,747` bytes。

本轮修复：

- `scripts/run_cyclegan_pretrained_style.sh` 原先即使本地已有 checkpoint 也会执行 `wget -O`，网络中断时会覆盖已有权重。
- 已改为：本地权重大小达到阈值时直接复用；缺失或明显不完整时下载到 `.download` 临时文件，成功后再替换。
- 新增回归测试：`test_cyclegan_script_reuses_existing_checkpoint`。

页面 API 验证：

- `/run_anime` 输入同一张测试脸。
- AnimeGANv2 选择三权重：`Face Paint v2`、`Face Paint v1`、`Paprika`。
- `include_cyclegan=True`，追加 CycleGAN Van Gogh。
- 成功输出目录：`outputs/facegan_studio/anime/20260507_181051`
- Gallery 返回 4 张：
  - 3 张 AnimeGANv2 输出 `(512, 512)`
  - 1 张 CycleGAN fake 输出 `(256, 256)`
- 拼图：`anime_grid.png` `(1048, 256)`
- 报告素材目录：`report/report_assets/facegan_studio/anime/20260507_181051`

本轮另一个修复：

- 三个 AnimeGANv2 输出都叫 `input_face.png`，同步到报告素材时曾互相覆盖。
- 已修 `facegan_studio/modules/image_utils.py::copy_files`，遇到同名文件自动追加 `_1`、`_2` 后缀。
- 新增回归测试：`test_copy_files_preserves_duplicate_basenames`。
- 修复后报告素材保留：
  - `input_face.png`
  - `input_face_1.png`
  - `input_face_2.png`
  - `input_face_3.png`
  - `input_face_fake.png`
  - `anime_grid.png`
  - `metadata.json`

## 8. InstantID

本地资源验证：

- `external/InstantID/checkpoints/ControlNetModel/diffusion_pytorch_model.safetensors` 存在，约 2.5GB。
- `external/InstantID/checkpoints/ip-adapter.bin` 存在，约 1.69GB。
- `external/InstantID/models/antelopev2/` 存在，包含 5 个 ONNX 文件。
- 本地 SDXL 基础模型存在：`/root/autodl-fs/models/YamerMIX_v8/model_index.json`
- `onnxruntime.get_available_providers()` 包含 `CUDAExecutionProvider`。

页面 API 验证：

- `/run_pose` 输入同一张测试脸。
- 参数：`count=4`，`strength_label='标准'`。
- 成功输出目录：`outputs/facegan_studio/pose_style/20260507_181205`
- 生成结果：
  - `pose_style_00_black_glasses.png` `(1024, 1024)`
  - `pose_style_01_metal_glasses.png` `(1024, 1024)`
  - `pose_style_02_business.png` `(1024, 1024)`
  - `pose_style_03_casual.png` `(1024, 1024)`
  - `pose_style_grid.png` `(1024, 256)`
  - `pose_reference_grid.png` `(1024, 256)`
- 已同步到：`report/report_assets/facegan_studio/pose_style/20260507_181205`
- metadata 记录：
  - `base_model`: `/root/autodl-fs/models/YamerMIX_v8`
  - `count`: `4`
  - `identity_strength`: `standard`
  - `outputs_count`: `4`
  - `pose_references_count`: `4`

说明：

- 本次验证使用从 StyleGAN3 展示图裁出的非个人测试脸，不代表最终个人照片效果。
- 由于当前 `data/` 未恢复，姿态参考回退为输入图自身，后续如需更多姿态应恢复或指定参考图来源。

## 9. GFPGAN

事实：

- 已安装并验证 `gfpgan==1.3.8`、`basicsr==1.4.2`、`facexlib==0.3.0`。
- `basicsr` 在当前 torchvision 环境下需要将 `functional_tensor` import 改为 `torchvision.transforms.functional`，细节见 `docs/handoff/后续模型注意事项.md`。
- 已跑通命令：

```bash
python scripts/run_gfpgan_postprocess.py --project-root . --default-inputs --limit 6 --sync-report --sync-handoff
```

- 实测输出：

```text
outputs/facegan_studio/gfpgan_postprocess/20260507_200337/gfpgan_comparison_grid.png
docs/handoff_assets/gfpgan_comparison_grid.png
```

结论：

- GFPGAN 已接入并可写作“人脸修复/清晰化后处理”。
- 不能写作身份保持模型；它可能改变局部五官细节。

## 9.1 本人照片轻造型保脸结果

用户反馈 StyleGAN 反演结果身份偏差太大，已取消该模块。为满足“人像不能失真和转换”的要求，新增轻造型保脸模块：

```bash
python scripts/run_identity_accessory_styles.py --project-root .
```

实测输出：

```text
outputs/facegan_studio/identity_accessory/20260507_204628/identity_accessory_grid.png
report/report_assets/facegan_studio/identity_accessory/20260507_204628/identity_accessory_grid.png
```

该图包含四张本人照片原图及眼镜叠加结果，只能保留在 ignored 的 `outputs/` 或 `report/report_assets/`，不要复制到 `docs/handoff_assets/`。

## 10. 当前遗留事项

- `check_sota_ready.py` 仍报告 `nvcc` 缺失，StyleGAN3 自定义算子可能受影响；本轮未验证 StyleGAN3 生成。
- FaceGAN Studio 服务启动后，InstantID 模型常驻导致显存占用约 18GB。如后续继续跑其他重模型，建议先重启服务或结束该进程释放显存。
- 当前 `data/`、`outputs/` 大目录没有从旧备份整体复制回新仓库；旧数据仍在 `/root/autodl-tmp/GAN_nogit_backup_20260507_175203`，需要时应按具体目录移动或软链接，避免再次复制占满磁盘。

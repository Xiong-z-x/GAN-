# 代码附录

## FaceGAN Studio 应用封装

新增目录 `facegan_studio/` 用于把课程项目成果封装成一个可交互 Web 程序。该程序不是把所有模块都称为 DCGAN，而是按功能分层调用不同模型：

- `facegan_studio/app.py`：Gradio 页面入口，提供上传预览、动漫风格化、证件照生成、造型与姿态生成、项目成果展示五个页面。
- `facegan_studio/modules/paths.py`：统一管理项目根目录、输出目录和报告素材目录。
- `facegan_studio/modules/image_utils.py`：负责读图、保存、拼图、复制素材和写入 metadata。
- `facegan_studio/modules/face_detector.py`：负责人脸检测预览。优先使用 OpenCV，人脸不可检测时用中心区域作为保守预览。
- `facegan_studio/modules/id_photo.py`：实现证件照裁剪、背景融合和轻度增强，输出白底、蓝底、红底结果。
- `facegan_studio/modules/anime_style.py`：调用已有 AnimeGANv2 脚本，生成 Face Paint v2、Face Paint v1、Paprika 三种动漫风格，可选追加 CycleGAN。
- `facegan_studio/modules/pose_styler.py`：调用 InstantID，固定输入人脸身份向量，生成戴眼镜、职业照、休闲照、不同姿态等结果。
- `facegan_studio/modules/gallery.py`：读取 `GAN_new_showcase_results/` 和旧报告素材，用于项目成果展示页。
- `scripts/run_facegan_studio.sh`：AutoDL 上的一键启动脚本。
- `tests/test_facegan_studio_core.py`：轻量单元测试，验证路径、拼图、证件照输出和展示素材兜底读取，不依赖重模型。

输出默认保存到 `outputs/facegan_studio/`，并同步到 `report/report_assets/facegan_studio/`，方便继续写入最终报告。

本附件用于解释本项目中**自写代码**和**整合脚本**的作用、输入输出与关键实现点。
说明范围如下：

- 只覆盖仓库内自己维护的代码和脚本
- `external/` 中的第三方官方仓库不展开源码细节，只记录调用方式
- 目标是让报告、最终交付包和代码工程三者保持一致

## 1. 环境与配置阶段

这一阶段负责把 AutoDL 环境和依赖关系理顺，保证后续所有脚本都能直接跑通。

| 文件 | 作用 | 输入 | 输出 | 关键点 |
|---|---|---|---|---|
| `requirements_autodl.txt` | 记录 AutoDL 上补装的 Python 依赖 | 当前镜像环境 | 可安装的依赖列表 | 只补缺失包，不重建整个环境 |
| `environment/env_main_gan.yml` | 主环境建议配置 | PyTorch / 训练脚本需求 | 环境文件 | 覆盖 DCGAN、素材整理、报告生成 |
| `environment/env_cycle.yml` | CycleGAN 官方仓库环境建议 | CycleGAN 运行需求 | 环境文件 | 对应官方测试脚本的依赖组合 |
| `environment/env_stylegan2ada_projector_optional.yml` | 可选 projector 环境建议 | StyleGAN2-ADA projector 需求 | 环境文件 | 仅在需要做投影时使用 |
| `scripts/install_autodl_dependencies.sh` | 检查并补装依赖 | AutoDL 基础镜像 | 可运行环境 | 先验证再安装，避免重复配置 |

## 2. 数据准备阶段

这一阶段负责把原始数据整理成适合课程作业的输入格式。

| 文件 | 作用 | 输入 | 输出 | 关键点 |
|---|---|---|---|---|
| `scripts/prepare_celeba.py` | 将 CelebA 原图整理成 64x64 训练集 | `data/raw/celeba/` | `data/processed/celeba_64/` | 统一 RGB、缩放、裁剪、命名 |
| `scripts/prepare_personal_photos.py` | 整理个人照片 | `data/raw/my_photos/` | `data/processed/style_transfer_inputs/` | 保留个人照片作为演示输入 |
| `scripts/prepare_style_transfer_inputs.py` | 准备动漫化与风格迁移输入 | 个人照片和 StyleGAN3 生成图 | `data/processed/style_transfer_inputs/` | 个人照片优先；不足时继续补充生成图 |

### 2.1 这一步为什么重要

1. DCGAN 需要统一尺寸和统一归一化，否则训练稳定性会变差。
2. AnimeGANv2 和 CycleGAN 需要输入图像已经是可直接推理的 PNG。
3. 个人照片只用于展示，不参与训练集构建，避免伦理和数据边界混乱。

## 3. 基础模型阶段：手写 DCGAN

这一阶段是课程知识的核心，也是本项目最重要的“自己动手”部分。

| 文件 | 作用 | 输入 | 输出 | 关键点 |
|---|---|---|---|---|
| `src/__init__.py` | Python 包入口 | 无 | 可导入的 `src` 包 | 仅负责包标记 |
| `src/dcgan/__init__.py` | DCGAN 子包入口 | 无 | 可导入的 `src.dcgan` 包 | 仅负责包标记 |
| `src/dcgan/dataset.py` | 数据集与变换 | 处理后的图像目录 | PyTorch Dataset | 递归读图，统一到 GAN 训练尺度 |
| `src/dcgan/models.py` | 生成器与判别器 | DCGANConfig | 生成器 / 判别器模型 | 使用经典卷积反卷积结构 |
| `src/dcgan/utils.py` | 工具函数 | 随机种子、目录路径、张量图像 | 日志、图像、JSON | 封装保存与复现相关逻辑 |
| `src/dcgan/train.py` | 训练入口 | 数据集、超参 | checkpoint、样例图、训练日志 | 交替优化 G/D，支持 baseline 与 DCGAN++ |

### 3.1 `dataset.py`

职责是把文件系统里的图像变成 PyTorch 可迭代的数据集。关键点是：

- 不依赖类别文件夹结构，直接递归读取图像；
- 统一为 RGB；
- 统一做 Resize、CenterCrop、归一化到 `[-1, 1]`；
- 把异常数据尽早暴露为 `FileNotFoundError`。

### 3.2 `models.py`

职责是定义 DCGAN 的生成器和判别器。关键点是：

- 生成器使用反卷积逐层上采样；
- 判别器使用卷积逐层下采样；
- 使用 DCGAN 论文常见的初始化方式；
- 将网络配置集中到 `DCGANConfig`，便于报告描述和超参复现实验。

### 3.3 `utils.py`

职责是把实验里常见的“杂项功能”统一收口。关键点是：

- 固定随机种子，保证实验可复现；
- 封装目录创建，避免脚本里反复写 `mkdir`；
- 保存 JSON、JSONL 和样例图，方便后续做报告图和日志分析；
- 使用固定噪声保存样例，便于观察训练过程变化。

### 3.4 `train.py`

职责是执行 DCGAN 的完整训练流程。关键点是：

- 先建数据集，再建 DataLoader；
- 判别器和生成器交替更新；
- 每轮保存样例和 checkpoint；
- 训练日志以 JSONL 追加写入，便于后续提取指标和过程图；
- 支持 `--max-steps`，方便快速调试。

### 3.5 `blocks.py`、`losses.py`、`augment.py`、`ema.py`

这一组文件是 DCGAN++ 的核心增强层，职责分别如下：

| 文件 | 作用 | 输入 | 输出 | 关键点 |
|---|---|---|---|---|
| `src/dcgan/blocks.py` | 残差块、自注意力、MinibatchStdDev | 中间特征图 | 更稳定的生成器与判别器模块 | 结构增强的主体 |
| `src/dcgan/losses.py` | BCE、Hinge、R1 损失 | 判别器输出与输入图像 | 标量损失 | 统一不同实验的损失形式 |
| `src/dcgan/augment.py` | DiffAugment | 真实图和生成图 | 增强后的图像 | 提升小规模训练的稳定性 |
| `src/dcgan/ema.py` | 生成器 EMA | 当前生成器参数 | EMA 生成器 | 用于更平滑、更好看的采样结果 |

### 3.6 `run_dcganpp_suite.sh`

这个脚本是 DCGAN++ 的总入口，负责按顺序跑完 E0 到 E4。它做的事情是：

- 自动检查 64 和 128 数据目录；
- 优先用本项目现有 CelebA 数据；
- 允许把高分辨率数据切换到 FFHQ thumbnails 128；
- 依次跑 baseline、稳定化版、结构增强版、128 版和 R1 细化版；
- 自动串联 checkpoint，减少人工干预。

## 4. 前沿复现与增强模块阶段

这一阶段不再从零训练大模型，而是复现公开仓库与预训练权重，保证两天内能稳定产出结果。

| 文件 | 作用 | 输入 | 输出 | 关键点 |
|---|---|---|---|---|
| `scripts/setup_external_repos.sh` | 克隆外部官方仓库 | 网络访问、Git | `external/` 下的官方仓库 | 只克隆，不改写外部源码 |
| `scripts/check_sota_ready.py` | 检查运行条件 | 当前环境、仓库路径 | 检查结果文本 | 先检查再运行，减少中途报错 |
| `scripts/run_stylegan3_generate.sh` | StyleGAN3 生成真实人像 | 官方预训练权重 | `outputs/stylegan3/images/` | 主打高质量真实人像 |
| `scripts/run_stylegan3_video.sh` | StyleGAN3 插值视频 | 生成图和潜空间设置 | `outputs/stylegan3/latent_interpolation.mp4` | 用于展示潜空间连续性 |
| `scripts/run_animegan2_infer.sh` | AnimeGANv2 动漫化 | 个人照片或生成图 | `outputs/animegan2*/` | 负责动画风格增强 |
| `scripts/run_cyclegan_pretrained_style.sh` | CycleGAN 风格迁移 | 输入图和官方风格权重 | `outputs/cyclegan_style*/` | 负责艺术风格化展示 |
| `scripts/run_stylegan2ada_projector_optional.sh` | 可选 projector | 目标人像 | projector 结果目录 | 作为加分模块，不作为主线硬要求 |
| `scripts/run_sota_enhanced_pipeline.sh` | 一键执行整条 SOTA 流程 | 上述各模块 | 完整的增强结果 | 把顺序依赖写成脚本，减少手工操作 |
| `scripts/verify_sota_outputs.py` | 校验输出 | 结果目录 | 校验报告 | 确认关键文件是否真的生成 |

### 4.1 这一阶段的设计原则

1. 先用官方预训练权重，避免长时间从零训练。
2. 把动漫化和风格迁移当成增强模块，不和 DCGAN baseline 混淆。
3. 所有外部仓库只调用入口脚本，不修改源码，以降低维护成本。

## 5. 报告与素材整理阶段

这一阶段负责把结果变成最终能交作业的材料。

| 文件 | 作用 | 输入 | 输出 | 关键点 |
|---|---|---|---|---|
| `scripts/rebuild_report_figures.py` | 重排报告插图 | `outputs/` 与 `final_images_package/` | `report/selected_figures/` | 用更清晰的 4x4、2x4 图替代拥挤拼图 |
| `scripts/collect_report_assets.py` | 汇总报告素材 | 各模块输出目录 | `report/report_assets/` | 按来源统一收集，避免文件名冲突 |
| `scripts/build_final_report_docx.py` | 导出最终报告 | Markdown 报告、素材图 | Word / PDF、最终包 | 同步正文、附件和清单到最终交付目录 |

### 5.1 `rebuild_report_figures.py`

这个脚本不是模型脚本，而是**报告排版脚本**。它做的事情是：

- 从训练过程图中提取代表性单元；
- 把 DCGAN 的训练过程排成更适合阅读的 4x4 拼图；
- 把 StyleGAN3、AnimeGANv2、CycleGAN 的结果整理成更清晰的对照图；
- 避免 8x8 小图过密导致报告里看不清细节。

### 5.2 `collect_report_assets.py`

这个脚本负责收集所有可用于报告的图片和视频。关键点是：

- 按来源目录统一前缀命名；
- 这样即使不同模块有同名文件，也不会覆盖；
- 最终素材目录可以直接用于写报告和打包提交。

### 5.3 `build_final_report_docx.py`

这个脚本负责把 Markdown 报告导出成 Word / PDF。关键点是：

- 自动排版封面、目录、标题、表格和图片；
- 把最终版报告和附件同步到 `最终报告/` 和最终结果包；
- 让交付文件不依赖手工复制，减少漏文件风险。

## 6. 与外部仓库的边界

以下目录属于第三方官方仓库，只调用，不改写：

- `external/stylegan3/`
- `external/stylegan2-ada-pytorch/`
- `external/pytorch-CycleGAN-and-pix2pix/`
- `external/animegan2-pytorch/`
- `external/contrastive-unpaired-translation/`

这些目录不在本附件的“自写代码”范围内。
报告里可以说明其来源与用途，但不需要把第三方源码当作自己的代码描述。

## 7. 建议的阅读顺序

如果是为了交作业或后续接手项目，建议按下面顺序读：

1. `README.md`
2. `技术路线_最终方案.md`
3. `代码附录.md`
4. `文件说明.md`
5. `GAN_真实人像生成实验报告.md`

这样可以先看整体路线，再看代码分工，最后看结果与附件。

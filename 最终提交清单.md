# 最终提交清单

建议提交内容分为“报告正文”“代码工程”“结果素材”三类。

## 1. 报告正文

| 文件 | 状态 | 说明 |
|---|---|---|
| `最终报告/GAN_真实人像生成实验报告_最终版.docx` | 已完成 | 已嵌入报告图的 Word 最终版 |
| `最终报告/GAN_真实人像生成实验报告_最终版.pdf` | 已完成 | 由 Word 导出的 PDF 最终版 |
| `GAN_真实人像生成实验报告.md` | 已完成 | 可作为 Word/PDF 报告底稿 |
| `报告素材使用说明.md` | 已完成 | 说明每类图像和视频如何放入报告 |
| `技术路线_最终方案.md` | 已完成 | 设计文档与技术路线依据 |
| `项目自检报告.md` | 已完成 | 进入 AutoDL 前的自检记录 |
| `项目完成进度.md` / `progress.md` | 已完成 | 项目过程记录 |

## 2. 代码工程

建议提交 GitHub 仓库中的代码工程，不提交大体积数据集、外部仓库、训练输出和权重。

核心代码包括：

| 路径 | 说明 |
|---|---|
| `src/dcgan/models.py` | 手写 DCGAN 生成器和判别器 |
| `src/dcgan/train.py` | DCGAN 训练入口 |
| `src/dcgan/dataset.py` | CelebA 图像读取与预处理 |
| `scripts/prepare_celeba.py` | CelebA 数据预处理 |
| `scripts/run_stylegan3_generate.sh` | StyleGAN3 官方预训练生成 |
| `scripts/run_stylegan3_video.sh` | StyleGAN3 插值视频 |
| `scripts/run_animegan2_infer.sh` | AnimeGANv2 推理 |
| `scripts/run_cyclegan_pretrained_style.sh` | CycleGAN 预训练风格迁移 |
| `scripts/collect_report_assets.py` | 报告素材收集 |

## 3. 结果素材

结果素材目录：

```text
C:\GAN\GAN_results_images\final_images_package
```

建议提交或备份：

| 路径 | 说明 |
|---|---|
| `outputs/dcgan/report_progress/` | DCGAN 训练过程图 |
| `outputs/dcgan/report_grids/` | DCGAN 生成样例 |
| `outputs/dcgan/fid_dcgan_vs_celeba_10k.txt` | DCGAN 最终 FID |
| `outputs/stylegan3/images/` | StyleGAN3 生成图 |
| `outputs/stylegan3/latent_interpolation.mp4` | StyleGAN3 插值视频 |
| `outputs/anime_report_selected/` | 动漫化与风格迁移精选素材 |
| `report/selected_figures/` | 已排版好的报告插图，推荐优先使用 |
| `report/report_assets/` | 汇总后的报告素材 |

## 4. 不建议提交的内容

| 内容 | 原因 |
|---|---|
| `img_align_celeba.zip` | 数据集过大，且应通过来源说明获取 |
| `data/processed/celeba_64/` | 中间数据，数量大，可由脚本复现 |
| `external/` | 第三方官方仓库，可由脚本重新 clone |
| `.pth/.pt/.pkl/.ckpt` 权重 | 体积大；当前轻量结果包已排除 |
| AutoDL 缓存目录 | 与实验结果无关 |

## 5. 最终报告应强调的结论

1. DCGAN 是自己手写并在 CelebA_64 上训练的 baseline，体现课程 GAN 基础。
2. StyleGAN3 是官方 FFHQ 预训练权重复现，体现高质量 GAN 工程复现能力。
3. AnimeGANv2 和 CycleGAN 是增强模块，不参与真实人像生成主指标排序。
4. DCGAN 的主 FID 是 `23.1820`；StyleGAN3 的跨域 FID 不作为公平排序依据。

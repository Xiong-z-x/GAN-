# 文件说明

更新时间：2026-04-30

本文件记录当前项目中各目录和文件的意义。后续新增、删除、移动文件时必须同步更新。

## 根目录文档

| 路径 | 作用 |
|---|---|
| `README.md` | 项目总说明，记录任务目标、执行顺序和关键边界 |
| `技术路线_最终方案.md` | 完整技术路线，包含模型选择、数据策略、风险控制和报告结构 |
| `项目完成进度.md` | 项目进度跟踪，记录已完成和待完成任务 |
| `AutoDL_运行指南.md` | 迁移到 AutoDL 后的一键运行、分步运行和常见问题说明 |
| `task_plan.md` | 任务计划文件，用于跨会话恢复执行状态 |
| `findings.md` | 调研发现与事实核验记录 |
| `progress.md` | 会话进度日志 |
| `repo_urls.md` | 官方仓库和数据集链接清单 |
| `文件说明.md` | 当前文件自身，记录各文件意义 |
| `requirements_autodl.txt` | AutoDL 环境中需要补装的非 PyTorch Python 依赖 |

## 环境文件

| 路径 | 作用 |
|---|---|
| `environment/env_main_gan.yml` | 主环境建议，覆盖 DCGAN、StyleGAN3、AnimeGANv2 和报告整理脚本 |
| `environment/env_cycle.yml` | CycleGAN 官方仓库运行环境建议 |
| `environment/env_stylegan2ada_projector_optional.yml` | 可选 StyleGAN2-ADA projector 环境建议 |

## 数据目录

| 路径 | 作用 |
|---|---|
| `data/raw/celeba/` | 原始 CelebA 数据或下载后的对齐裁剪数据 |
| `data/raw/ffhq_thumbnails/` | FFHQ 128 缩略图或轻量子集 |
| `data/raw/metfaces_optional/` | 可选 MetFaces 数据 |
| `data/raw/my_photos/` | 个人照片原图，仅用于 demo |
| `data/processed/celeba_64/` | DCGAN 使用的 64x64 CelebA 图像 |
| `data/processed/my_photos_aligned/` | 对齐后的人脸图像 |
| `data/processed/style_transfer_inputs/` | 风格迁移输入图像 |
| `data/processed/style_transfer_input_backup/` | 自动输入准备脚本保存的来源示例备份 |

## 源码目录

| 路径 | 作用 |
|---|---|
| `src/dcgan/models.py` | 手写 DCGAN 生成器、判别器与权重初始化 |
| `src/dcgan/dataset.py` | DCGAN 数据读取与图像变换 |
| `src/dcgan/train.py` | DCGAN 训练入口 |
| `src/dcgan/utils.py` | 保存图像、随机种子、输出目录等工具函数 |
| `src/__init__.py` | 项目源码包初始化文件 |
| `src/dcgan/__init__.py` | Python 包初始化文件 |

## 脚本目录

| 路径 | 作用 |
|---|---|
| `scripts/prepare_celeba.py` | 将 CelebA 图像整理为训练所需分辨率 |
| `scripts/prepare_personal_photos.py` | 整理个人照片，进行基础筛选和复制 |
| `scripts/prepare_style_transfer_inputs.py` | 自动准备 AnimeGANv2 和 CycleGAN 输入，优先使用个人照片，否则使用 StyleGAN3 生成图 |
| `scripts/install_autodl_dependencies.sh` | 检查 PyTorch 环境并安装 AutoDL 运行所需依赖 |
| `scripts/check_sota_ready.py` | 检查 SOTA 与增强模块运行条件 |
| `scripts/run_stylegan3_generate.sh` | 调用 StyleGAN3 官方预训练权重生成真实人像 |
| `scripts/run_stylegan3_video.sh` | 调用 StyleGAN3 生成潜空间插值视频 |
| `scripts/run_animegan2_infer.sh` | 调用 AnimeGANv2 预训练权重进行动漫化推理 |
| `scripts/run_cyclegan_pretrained_style.sh` | 调用 CycleGAN 官方预训练风格模型做风格迁移 |
| `scripts/run_stylegan2ada_projector_optional.sh` | 可选调用 StyleGAN2-ADA projector |
| `scripts/run_sota_enhanced_pipeline.sh` | AutoDL 上一键执行 StyleGAN3、AnimeGANv2、CycleGAN 和报告素材收集 |
| `scripts/verify_sota_outputs.py` | 检查 SOTA 与增强模块输出是否存在 |
| `scripts/collect_report_assets.py` | 收集关键结果图到报告目录 |
| `scripts/setup_external_repos.sh` | 在 AutoDL 上克隆外部官方仓库 |

## 外部仓库目录

| 路径 | 作用 |
|---|---|
| `external/stylegan3/` | NVIDIA StyleGAN3 官方仓库，运行预训练人像生成 |
| `external/stylegan2-ada-pytorch/` | NVIDIA StyleGAN2-ADA 官方仓库，可选 projector |
| `external/pytorch-CycleGAN-and-pix2pix/` | CycleGAN/pix2pix 官方仓库 |
| `external/animegan2-pytorch/` | AnimeGANv2 PyTorch 仓库 |
| `external/contrastive-unpaired-translation/` | CUT/FastCUT 备用仓库 |

## 输出与报告目录

| 路径 | 作用 |
|---|---|
| `outputs/dcgan/` | DCGAN 训练输出 |
| `outputs/stylegan3/` | StyleGAN3 生成输出 |
| `outputs/animegan2/` | AnimeGANv2 动漫化输出 |
| `outputs/cyclegan_style/` | CycleGAN 风格迁移输出 |
| `outputs/stylegan2ada_projector_optional/` | 可选 projector 输出 |
| `report/report_outline.md` | 报告骨架 |
| `report/results_summary.md` | 实验结果汇总 |
| `report/report_assets/` | 报告使用的图片和视频素材 |

## 本地学习记录

| 路径 | 作用 |
|---|---|
| `.learnings/LEARNINGS.md` | 记录用户纠偏和重要经验 |
| `.learnings/ERRORS.md` | 记录执行过程中的错误 |
| `.learnings/FEATURE_REQUESTS.md` | 记录后续新增需求 |

# 报告素材使用说明

本文档说明 `C:\GAN\GAN_results_images\final_images_package` 中各类结果素材的用途，便于后续撰写 Word 报告、PPT 展示或整理提交附件。

## 1. 总体目录

```text
GAN_results_images/final_images_package/
  outputs/        实验输出图片、视频和指标文本
  report/         报告素材汇总与说明
  README.md       项目说明
  AutoDL_Run_Guide.md  AutoDL 运行简表
  file_manifest.txt    已重新生成的文件清单
```

当前轻量包不包含 `.pth`、`.pt`、`.pkl`、`.ckpt` 权重文件，适合作为报告素材包。

已生成最终报告文件：

| 路径 | 用途 |
|---|---|
| `最终报告/GAN_真实人像生成实验报告_最终版.docx` | 本地 Word 最终版，已嵌入 5 张报告图 |
| `最终报告/GAN_真实人像生成实验报告_最终版.pdf` | 本地 PDF 最终版 |
| `GAN_results_images/final_images_package/GAN_真实人像生成实验报告_最终版.docx` | 结果包内 Word 最终版 |
| `GAN_results_images/final_images_package/GAN_真实人像生成实验报告_最终版.pdf` | 结果包内 PDF 最终版 |

## 2. DCGAN 素材

| 路径 | 用途 |
|---|---|
| `report/selected_figures/fig01_dcgan_progress.png` | 已排版好的 DCGAN 训练递进图，推荐直接放入报告 |
| `report/selected_figures/fig02_dcgan_generated_grids.png` | 已排版好的 DCGAN 生成样例图，推荐直接放入报告 |
| `outputs/dcgan/report_progress/dcgan_epoch_001.png` | 展示 DCGAN 早期训练效果 |
| `outputs/dcgan/report_progress/dcgan_epoch_060.png` | 展示 DCGAN 60 epoch 后效果 |
| `outputs/dcgan/report_grids/dcgan_grid_00.png` | 报告中推荐使用的 DCGAN 生成样例图 |
| `outputs/dcgan/samples/` | 训练过程中保存的固定噪声生成图 |
| `outputs/dcgan/fid_dcgan_vs_celeba_10k.txt` | 最终推荐写入报告的 DCGAN FID |
| `outputs/dcgan/fid_dcgan_vs_celeba.txt` | 初次 4096 张生成图 FID，作为辅助记录 |

报告中建议主写：

```text
DCGAN 在 CelebA_64 上训练 60 epoch，使用 10000 张生成图计算 FID，得到 FID = 23.1820。
```

## 3. StyleGAN3 素材

| 路径 | 用途 |
|---|---|
| `report/selected_figures/fig03_stylegan3_samples.png` | 已排版好的 StyleGAN3 生成样例矩阵，推荐直接放入报告 |
| `outputs/stylegan3/images/` | StyleGAN3 官方 FFHQ 预训练权重生成的 1024 张真实人像 |
| `outputs/stylegan3/images/seed0000.png` | 报告示例图，可作为单图展示 |
| `outputs/stylegan3/images/seed0001.png` | 报告示例图 |
| `outputs/stylegan3/images/seed0002.png` | 报告示例图 |
| `outputs/stylegan3/latent_interpolation.mp4` | 潜空间插值视频，适合 PPT 展示 |
| `outputs/stylegan3/fid_stylegan3_vs_celeba.txt` | 跨域 FID 参考，不用于公平排序 |

报告中必须说明：

```text
StyleGAN3 使用 FFHQ 官方预训练权重，而 FID 真实集是 CelebA_64，因此该 FID 只是跨域参考。
```

## 4. AnimeGANv2 素材

| 路径 | 用途 |
|---|---|
| `report/selected_figures/fig04_animegan2_comparison.png` | 已排版好的 AnimeGANv2 多风格对比图，推荐直接放入报告 |
| `outputs/anime_report_selected/original/` | 动漫化和风格迁移输入图 |
| `outputs/anime_report_selected/face_paint_v2/` | 推荐展示的强动漫化结果 |
| `outputs/anime_report_selected/face_paint_v1/` | 对比风格 |
| `outputs/anime_report_selected/paprika/` | 对比风格 |
| `outputs/animegan2_compare/` | 三种 AnimeGANv2 风格的完整对比集合 |

报告中建议使用 `original/anime_input_000.png` 和 `face_paint_v2/anime_input_000.png` 做一组前后对比。

## 5. CycleGAN 素材

| 路径 | 用途 |
|---|---|
| `report/selected_figures/fig05_cyclegan_ukiyoe_comparison.png` | 已排版好的 CycleGAN Ukiyoe 对比图，推荐直接放入报告 |
| `outputs/cyclegan_style/` | Van Gogh 风格迁移结果 |
| `outputs/cyclegan_style_monet/` | Monet 风格迁移结果 |
| `outputs/cyclegan_style_ukiyoe/` | Ukiyoe 风格迁移结果 |
| `outputs/anime_report_selected/ukiyoe/` | 已整理好的 Ukiyoe 报告精选图 |

报告中建议主展示 Ukiyoe，因为它的人像风格变化最明显；Monet 和 Van Gogh 可作为附录或 PPT 补充。

## 6. 报告写作建议

主报告建议按以下顺序放图：

1. DCGAN 训练早期与最终结果对比。
2. DCGAN 生成样例网格。
3. StyleGAN3 高质量人像样例。
4. StyleGAN3 潜空间插值视频截图或视频链接。
5. AnimeGANv2 原图与动漫化结果对比。
6. CycleGAN 原图与 Ukiyoe 风格结果对比。

不要把 AnimeGANv2/CycleGAN 的结果放进真实人像生成 FID 主表；它们是增强模块，主要做定性分析。
